{
  "name": "frontend",
  "version": "1.0.0",
  "main": "index.js",
 "scripts": {
    "dev": "npx knex migrate:rollback && npx knex migrate:latest && npx knex seed:run && nodemon server.js",
    "start": "npx knex migrate:latest && npx knex seed:run && node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
